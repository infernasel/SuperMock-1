# SuperMock - Локальный Mock-сервер для Telegram Bot API

🚀 **SuperMock** - это мощный локальный mock-сервер для тестирования Telegram-ботов без подключения к реальному Telegram API. Он предоставляет полную mock-реализацию Telegram Bot API и включает интерактивный терминальный интерфейс чата для ручного тестирования.

## ✨ Возможности

- 🎯 **Полный Mock Bot API**: Реализует основные эндпоинты Telegram Bot API
- 💬 **Интерактивный терминальный чат**: Тестируйте своего бота с терминальным интерфейсом, похожим на Telegram
- 🔄 **Поддержка Long Polling**: Совместим с ботами, использующими `getUpdates`
- 🪝 **Поддержка Webhook**: Mock-эндпоинты для тестирования webhook
- 📚 **Совместимость с библиотеками**: Работает с популярными библиотеками для Telegram ботов:
  - `python-telegram-bot`
  - `aiogram`
  - `pyTelegramBotAPI` (telebot)
  - И любыми другими библиотеками, использующими стандартный Bot API
- 🧪 **Готов для тестирования**: Идеально подходит для автоматического тестирования и CI/CD пайплайнов
- 🔧 **Простая настройка**: Простая установка и конфигурация

## 📦 Установка

### Из исходников:

```bash
git clone https://github.com/infernasel/SuperMock-1.git
cd SuperMock-1
pip install -e .
```

### Требования:

- Python 3.7+
- Flask 2.0+
- Requests 2.25+

## 🚀 Быстрый старт

### 1. Запуск Mock-сервера (для автоматического тестирования)

```bash
supermock server
```

Сервер запустится по адресу `http://localhost:8081`

### 2. Запуск интерактивного терминального чата

```bash
supermock chat
```

Это запустит mock-сервер и интерактивный терминальный интерфейс, где вы сможете общаться с вашим ботом.

### 3. Настройка вашего бота

Настройте ваш бот для использования mock-сервера вместо реального Telegram API:

#### Для python-telegram-bot:

```python
from telegram.ext import Application

# Используйте URL mock-сервера
application = Application.builder() \
    .token("YOUR_TOKEN") \
    .base_url("http://localhost:8081/bot") \
    .build()
```

#### Для aiogram:

```python
from aiogram import Bot, Dispatcher

# Используйте URL mock-сервера
bot = Bot(token="YOUR_TOKEN", 
          base_url="http://localhost:8081/bot{token}")
```

#### Для pyTelegramBotAPI (telebot):

```python
import telebot

# Используйте URL mock-сервера
telebot.apihelper.API_URL = "http://localhost:8081/bot{0}/{1}"
bot = telebot.TeleBot("YOUR_TOKEN")
```

## 📖 Примеры использования

### Пример 1: Простой тест эхо-бота

```python
# bot.py
from telegram.ext import Application, CommandHandler, MessageHandler, filters

async def start(update, context):
    await update.message.reply_text("Привет! Я тестовый бот.")

async def echo(update, context):
    await update.message.reply_text(update.message.text)

def main():
    application = Application.builder() \
        .token("test_token") \
        .base_url("http://localhost:8081/bot") \
        .build()

    application.add_handler(CommandHandler("start", start))
    application.add_handler(MessageHandler(filters.TEXT & ~filters.COMMAND, echo))

    application.run_polling()

if __name__ == '__main__':
    main()
```

**Терминал 1**: Запустите SuperMock
```bash
supermock chat
```

**Терминал 2**: Запустите вашего бота
```bash
python bot.py
```

Теперь вы можете взаимодействовать с вашим ботом в Терминале 1!

### Пример 2: Автоматическое тестирование

```python
# test_bot.py
import pytest
from supermock.api import TelegramMockServer
import threading
import time

@pytest.fixture
def mock_server():
    """Настройка mock-сервера для тестирования"""
    server = TelegramMockServer(port=8082)
    thread = threading.Thread(target=lambda: server.run(), daemon=True)
    thread.start()
    time.sleep(1)  # Подождите запуска сервера
    yield server

def test_bot_responds_to_start(mock_server):
    """Проверка, что бот отвечает на команду /start"""
    # Отправить команду /start
    mock_server.send_user_message("/start")
    
    # Подождать обработки ботом
    time.sleep(1)
    
    # Проверить историю сообщений
    messages = mock_server.get_messages_history()
    
    # Найти ответ бота
    bot_messages = [m for m in messages if m["type"] == "bot"]
    assert len(bot_messages) > 0
    assert "Привет" in bot_messages[0]["message"]["text"]
```

## 🎮 Поддерживаемые методы API

SuperMock в настоящее время поддерживает следующие методы Telegram Bot API:

- ✅ `getMe` - Получение информации о боте
- ✅ `getUpdates` - Получение входящих обновлений (long polling)
- ✅ `setWebhook` - Установка URL webhook
- ✅ `deleteWebhook` - Удаление webhook
- ✅ `getWebhookInfo` - Получение информации о webhook
- ✅ `sendMessage` - Отправка текстовых сообщений
- ✅ `sendPhoto` - Отправка фотографий
- ✅ `sendDocument` - Отправка документов
- ✅ `editMessageText` - Редактирование текстовых сообщений
- ✅ `answerCallbackQuery` - Ответ на callback-запросы от inline-клавиатур

При необходимости можно легко добавить больше методов!

## 🔧 Параметры конфигурации

### Параметры сервера

```bash
supermock server --host 0.0.0.0 --port 8080 --debug
```

- `--host`: Хост для привязки сервера (по умолчанию: localhost)
- `--port`: Порт для привязки сервера (по умолчанию: 8081)
- `--debug`: Включить режим отладки Flask

### Параметры чата

```bash
supermock chat --host localhost --port 8081
```

- `--host`: Хост для привязки сервера (по умолчанию: localhost)
- `--port`: Порт для привязки сервера (по умолчанию: 8081)

## 🧪 Запуск тестов

```bash
# Установить зависимости для разработки
pip install -r requirements-dev.txt

# Запустить тесты
pytest tests/
```

## 📝 Лицензия

Этот проект лицензирован под Apache License 2.0 - подробности см. в файле LICENSE.

## 🙏 Благодарности

- Вдохновлен необходимостью локального тестирования ботов без квот API
- Создан для поддержки сообщества разработчиков Telegram-ботов

## 📧 Поддержка

По вопросам, проблемам или предложениям, пожалуйста, посетите:
- GitHub Issues: https://github.com/infernasel/SuperMock-1/issues

---

Сделано с ❤️ для разработчиков Telegram-ботов
